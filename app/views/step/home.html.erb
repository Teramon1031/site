<div class="top top-wrapper">
  <div id="top-text">
    <h1 id="top-h1">STEPの記録</h1>
    <p id="top-p"></p>
  </div>
</div>
<div class="container">
  <div class="subpage-title">
    <h2>STEPについて</h2>
  </div>
  <div class="contents">
    <p>徒然なるままに書いたとりとめのない日記たち。</p>
    <div class="card">
      <ul>
        <% @diaries.each do |diary| %>
          <%=link_to("/step/#{diary.id}") do%>
            <li>
              <span class="fa fa-pen-nib"></span>
              <p class="title01"><%=diary.title%></p>
              <p><%=diary.short%></p>
              <span class="next">> 続きを読む</span>
            </li>
          <%end%>
        <% end %>
      </ul>
    </div>
  </div>
</div>
